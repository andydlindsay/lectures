import ProductListItem from "./ProductListItem";

const ProductList = () => {
  const products = [];

  return (
    <div>
      <h2>ProductList component</h2>
    </div>
  );
};

export default ProductList;
